Based off of the lovely tutorial at howtographql.com

To start the server, run `yarn start` in `/server`.
To start the client, run `yarn start` in `/client`. (be sure to do this in a separate command tabs)
